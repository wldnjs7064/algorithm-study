#include <iostream>
using namespace std;

int main()
{
	int sum;
	int a, b, c, d, e, f, g, h, i;
	int value;
	cin >> sum >> a >> b >> c >> d >> e >> f >> g >> h >> i;
	value = sum - (a + b + c + d + e + f + g + h + i);
	cout << value;
}